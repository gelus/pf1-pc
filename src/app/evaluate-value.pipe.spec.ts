import { EvaluateValuePipe } from './evaluate-value.pipe';

describe('EvaluateValuePipe', () => {
  it('create an instance', () => {
    const pipe = new EvaluateValuePipe();
    expect(pipe).toBeTruthy();
  });
});
