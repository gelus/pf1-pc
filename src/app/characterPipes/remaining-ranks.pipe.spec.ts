import { RemainingRanksPipe } from './remaining-ranks.pipe';

describe('RemainingRanksPipe', () => {
  it('create an instance', () => {
    const pipe = new RemainingRanksPipe();
    expect(pipe).toBeTruthy();
  });
});
