import { RemainingStartingWealthPipe } from './remaining-starting-wealth.pipe';

describe('RemainingStartingWealthPipe', () => {
  it('create an instance', () => {
    const pipe = new RemainingStartingWealthPipe();
    expect(pipe).toBeTruthy();
  });
});
