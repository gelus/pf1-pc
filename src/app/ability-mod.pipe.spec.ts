import { AbilityModPipe } from './ability-mod.pipe';

describe('AbilityModPipe', () => {
  it('create an instance', () => {
    const pipe = new AbilityModPipe();
    expect(pipe).toBeTruthy();
  });
});
